spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chaptgen
    username: chaptgen
    password: chaptgen
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration

youtubeTranscript:
  apiKey: "apikey here (hidden)"

gemini:
  apiKey: "apikey here (hidden)"

auth:
  jwtSecret: "change-me-dev-secret"
  jwtDays: 14
  cookieName: "chaptgen_token"
  cookieDomain: "localhost"